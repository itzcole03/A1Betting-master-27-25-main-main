import type { Sport, PropType} from '@/types/core.ts';
interface UsePropsOptions {
  autoRefresh?: boolean;
  refreshInterval?: number;
  sport?: Sport;
  propType?: PropType;}
export declare const useProps: ({
  autoRefresh,
  refreshInterval,
  sport,
//   propType
}?: UsePropsOptions) => {
  props: PlayerProp[0],`n  opportunities: Opportunity[0];,`n  isLoading: boolean,`n  error: Error | null;,`n  refreshProps: () => void};
export Record<string, any>;


`
